name: coding_agents
description: Dart adapters for CLI coding agents (Claude Code, Codex CLI, Gemini CLI).
version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  json_annotation: ^4.9.0
  path: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.14
  json_serializable: ^6.9.4
  lints: ^6.0.0
  test: ^1.25.6
  mocktail: ^1.0.3
